/// Keep all hardcoded error messages that come back from the API here.
/// So that we can localize them on the app side before they are displayed to
/// the user.
library;

String kLoginError = 'Username/Password Not Found';
